% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudolabel.R
\name{create_synthetic_similarity_matrix}
\alias{create_synthetic_similarity_matrix}
\title{Create synthetic similarity matrix for testing pseudolabeling}
\usage{
create_synthetic_similarity_matrix(
  n_samples = 1000,
  n_clusters = 20,
  within_cluster_sim = 0.8,
  between_cluster_sim = 0.1,
  sparsity = 0.1,
  noise_level = 0.1
)
}
\arguments{
\item{n_samples}{Total number of samples}

\item{n_clusters}{Number of true clusters}

\item{within_cluster_sim}{Average similarity within clusters (default: 0.8)}

\item{between_cluster_sim}{Average similarity between clusters 
(default: 0.1)}

\item{sparsity}{Overall sparsity level (proportion of non-zero entries) 
(default: 0.1)}

\item{noise_level}{Amount of noise to add to similarities (default: 0.1)}
}
\value{
A list containing:
  \item{sim_matrix}{Sparse similarity matrix (dgCMatrix) with synthetic 
    cluster structure}
  \item{true_labels}{Factor vector of true cluster assignments for 
    validation}
  \item{cluster_centers}{Integer vector of cluster center indices}
}
\description{
Generates a synthetic sparse similarity matrix with known cluster structure. 
Used for testing and demonstrating the pseudolabeling functions.
}
\examples{
\donttest{
# Create synthetic data
synthetic <- create_synthetic_similarity_matrix(n_samples = 500, 
                                                n_clusters = 10)

# Apply pseudolabeling
result <- assign_pseudolabels(synthetic$sim_matrix, verbose = TRUE)

# Compare with true labels
table(result$labels, synthetic$true_labels, useNA = "always")
}

}
