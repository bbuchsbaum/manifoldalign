% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{uot_apply_map_sparse_mat_cpp}
\alias{uot_apply_map_sparse_mat_cpp}
\title{Apply UOT coupling as a barycentric map (sparse, matrix signal via CSC)}
\usage{
uot_apply_map_sparse_mat_cpp(
  col_ptr,
  row_idx,
  cost_csc,
  n_rows,
  n_cols,
  alpha,
  beta,
  fbar,
  gbar,
  epsilon,
  signal,
  delta = 1e-08
)
}
\arguments{
\item{col_ptr}{Integer vector of length m+1 with CSC column offsets (0-based
or 1-based accepted; if 1-based, it must start at 0 or 1 and be nondecreasing).}

\item{row_idx}{Integer vector of length nnz giving 1-based row indices.}

\item{cost_csc}{Numeric vector of length nnz aligned with `row_idx`.}

\item{n_rows}{Number of source nodes.}

\item{n_cols}{Number of target nodes.}

\item{alpha}{Source masses (length n_rows).}

\item{beta}{Target masses (length n_cols).}

\item{fbar}{Translation-invariant source potential (length n_rows).}

\item{gbar}{Translation-invariant target potential (length n_cols).}

\item{epsilon}{Entropic regularization parameter (> 0).}

\item{signal}{Source signal matrix (T x n_rows).}

\item{delta}{Stabilizer added to the denominator.}
}
\value{
A numeric matrix (T x n_cols) in template space.
}
\description{
Sparse matrix version for time series or multi-contrast signals. Requires a
CSC representation of the cost graph (column pointers + row indices).
}
\details{
This computes \eqn{\hat S = S W / (\pi_2 + \delta)} without materializing
the full \eqn{\pi}. The implementation uses per-column log-stabilization to
avoid overflow in the exponential weights.
}
