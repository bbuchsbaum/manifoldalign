% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters_grasp.R, R/adapters_ot_procrustes.R,
%   R/align_framework.R
\name{latent_dim.grasp_pair_fit}
\alias{latent_dim.grasp_pair_fit}
\alias{latent_dim.ot_procrustes_pair_fit}
\alias{latent_dim}
\title{Latent dimension selected by the method (if applicable)}
\usage{
\method{latent_dim}{grasp_pair_fit}(fit, ...)

\method{latent_dim}{ot_procrustes_pair_fit}(fit, ...)

latent_dim(fit, ...)
}
\arguments{
\item{fit}{A fit object returned by fit_pair or fit_many}

\item{...}{Additional arguments passed to methods}
}
\value{
An integer giving the latent dimension selected by the method, or
  NULL if not applicable.
}
\description{
Latent dimension selected by the method (if applicable)
}
\examples{
\donttest{
set.seed(1)
X1 <- matrix(rnorm(50), 25, 2)
X2 <- matrix(rnorm(50), 25, 2)
algo <- grasp_aligner()
fit <- fit_pair(algo, X1, X2)
dim <- latent_dim(fit)
}
}
