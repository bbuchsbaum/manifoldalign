% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure_descriptors.R
\name{compute_graph_laplacian}
\alias{compute_graph_laplacian}
\title{Compute a (normalized) graph Laplacian from an adjacency matrix}
\usage{
compute_graph_laplacian(
  A,
  normalized = TRUE,
  symmetrize = TRUE,
  zero_diagonal = TRUE
)
}
\arguments{
\item{A}{Square adjacency/affinity matrix (dense or sparse). Values should be
nonnegative; the diagonal is set to 0 by default.}

\item{normalized}{Logical; if TRUE (default), compute the normalized Laplacian
\eqn{L = I - D^{-1/2} A D^{-1/2}}. If FALSE, compute the unnormalized
Laplacian \eqn{L = D - A}.}

\item{symmetrize}{Logical; if TRUE (default), symmetrize via `(A + t(A))/2`.}

\item{zero_diagonal}{Logical; if TRUE (default), set `diag(A)=0` before
forming the Laplacian.}
}
\value{
A sparse symmetric matrix (CsparseMatrix).
}
\description{
Compute a (normalized) graph Laplacian from an adjacency matrix
}
