% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_many.R
\name{rotation_sync}
\alias{rotation_sync}
\title{Rotation synchronization for orthogonal group O(k)}
\usage{
rotation_sync(G, W, E, N, k)
}
\arguments{
\item{G}{list of align_transform objects (group="O"), each a relative g_ij}

\item{W}{numeric weights per edge in E}

\item{E}{data.frame with columns i, j (1-based dataset indices)}

\item{N}{number of datasets}

\item{k}{latent dimension}
}
\value{
list of per-domain align_transform objects mapping to consensus
}
\description{
Rotation synchronization for orthogonal group O(k)
}
\examples{
\donttest{
# Rotation synchronization for orthogonal transformations
k <- 3
N <- 3
G <- list(matrix(rnorm(9), k, k), matrix(rnorm(9), k, k))
W <- c(1, 1)
E <- data.frame(i = c(1, 2), j = c(2, 3))
# result <- rotation_sync(G, W, E, N = N, k = k)
}
}
