% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters_grasp.R, R/adapters_ot_procrustes.R,
%   R/adapters_parrot.R, R/adapters_token_ot_graph.R, R/align_framework.R
\name{pair_loss.grasp_pair_fit}
\alias{pair_loss.grasp_pair_fit}
\alias{pair_loss.ot_procrustes_pair_fit}
\alias{pair_loss.parrot_pair_fit}
\alias{pair_loss.token_ot_graph_pair_fit}
\alias{pair_loss}
\title{Pairwise loss (for weighting/diagnostics)}
\usage{
\method{pair_loss}{grasp_pair_fit}(fit, X_i = NULL, X_j = NULL, ...)

\method{pair_loss}{ot_procrustes_pair_fit}(fit, X_i = NULL, X_j = NULL, ...)

\method{pair_loss}{parrot_pair_fit}(fit, X_i = NULL, X_j = NULL, ...)

\method{pair_loss}{token_ot_graph_pair_fit}(fit, X_i = NULL, X_j = NULL, ...)

pair_loss(fit, X_i = NULL, X_j = NULL, ...)
}
\arguments{
\item{fit}{A pairwise fit object returned by fit_pair}

\item{X_i}{Optional first domain data for loss computation}

\item{X_j}{Optional second domain data for loss computation}

\item{...}{Additional arguments passed to methods}
}
\value{
A single numeric value representing the pairwise alignment loss, or
  NA_real_ if not available.
}
\description{
Pairwise loss (for weighting/diagnostics)
}
\examples{
\donttest{
set.seed(1)
X1 <- matrix(rnorm(50), 25, 2)
X2 <- matrix(rnorm(50), 25, 2)
algo <- grasp_aligner()
fit <- fit_pair(algo, X1, X2)
loss <- pair_loss(fit)
}
}
