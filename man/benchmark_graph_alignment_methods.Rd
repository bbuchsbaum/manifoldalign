% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_alignment_benchmark.R
\name{benchmark_graph_alignment_methods}
\alias{benchmark_graph_alignment_methods}
\title{Benchmark graph alignment methods on synthetic permuted graphs}
\usage{
benchmark_graph_alignment_methods(
  sizes = c(50L, 100L),
  d = 3L,
  noise_sd = 0.05,
  structure = c("ring", "grid", "random", "community"),
  permute_fraction = 1,
  n_anchors = 0L,
  methods = c("token_ot_graph", "fpgw", "cone_align", "grasp", "parrot"),
  candidate_k = 80L,
  coarsen_method = c("kmeans", "louvain"),
  token_mode = c("view_only", "view_plus_neighbors"),
  views = "raw",
  n_levels = 1L,
  prior_mode = c("none", "hard", "soft"),
  fpgw_omega1 = 0.5,
  fpgw_epsilon = 0.01,
  fpgw_max_iter = 50L,
  fpgw_inner_max_iter = 20L,
  fpgw_tol = 1e-06,
  n_reps = 3L,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{sizes}{Integer vector of node counts to benchmark.}

\item{d}{Integer feature dimension for synthetic node features.}

\item{noise_sd}{Numeric noise standard deviation added to target features.}

\item{structure}{Synthetic geometry for base node coordinates:
`"ring"`, `"grid"`, `"random"`, or `"community"`.}

\item{permute_fraction}{Fraction of nodes to permute (in `(0,1]`).}

\item{n_anchors}{Integer number of anchor correspondences to add (0 allowed).}

\item{methods}{Character vector of methods to run. Options include:
`"token_ot_graph"`, `"cone_align"`, `"grasp"`, `"parrot"`, `"fpgw"`.}

\item{candidate_k}{Integer number of candidates per source node for
`token_ot_graph_align()`.}

\item{coarsen_method}{Coarsening method for multilevel Token-OT alignment:
`"kmeans"` or `"louvain"`.}

\item{token_mode}{Tokenization mode passed to [token_ot_graph_align_control()].}

\item{views}{Views passed to [token_ot_graph_align_control()].}

\item{n_levels}{Integer number of multilevel hierarchy levels for
`token_ot_graph_align()`.}

\item{prior_mode}{Prior lifting mode for multilevel (`"none"`, `"hard"`, `"soft"`).}

\item{fpgw_omega1}{Weight of the feature term for `fpgw()` (0–1).}

\item{fpgw_epsilon}{Entropic warm-start regularization for `fpgw()`.}

\item{fpgw_max_iter}{Maximum Frank-Wolfe iterations for `fpgw()`.}

\item{fpgw_inner_max_iter}{Inner iterations for `fpgw()` updates.}

\item{fpgw_tol}{Convergence tolerance for `fpgw()`.}

\item{n_reps}{Integer number of replications per size.}

\item{seed}{Integer seed for reproducibility.}

\item{verbose}{Logical; print per-run progress.}
}
\value{
A data.frame with per-run runtime and accuracy metrics.
}
\description{
Generates paired domains where the target is a permuted/noisy copy of the
source and compares multiple alignment methods available in this package.
}
\details{
Benchmarks are intended for interactive profiling and method comparison
rather than automated CI.
}
