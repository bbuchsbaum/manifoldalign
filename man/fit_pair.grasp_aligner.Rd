% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapters_grasp.R
\name{fit_pair.grasp_aligner}
\alias{fit_pair.grasp_aligner}
\title{Fit GRASP on a pair of domains}
\usage{
\method{fit_pair}{grasp_aligner}(
  algo,
  X_i,
  X_j,
  links = NULL,
  ncomp = 30,
  q_descriptors = 100,
  sigma = 0.73,
  lambda = 0.1,
  use_laplacian = TRUE,
  solver = c("linear", "hungarian", "auction"),
  ...
)
}
\arguments{
\item{algo}{A grasp_aligner object}

\item{X_i}{First domain data matrix (samples x features)}

\item{X_j}{Second domain data matrix (samples x features)}

\item{links}{Optional correspondence links (unused by GRASP)}

\item{ncomp}{Number of spectral components to compute}

\item{q_descriptors}{Number of descriptor vectors for alignment}

\item{sigma}{Bandwidth parameter for descriptor weighting}

\item{lambda}{Regularization parameter for rotation alignment}

\item{use_laplacian}{Logical; if TRUE use graph Laplacian, else adjacency}

\item{solver}{Assignment solver: "linear", "hungarian", or "auction"}

\item{...}{Additional arguments (unused)}
}
\value{
An object of class grasp_pair_fit containing rotation (kxk matrix),
  mapping_matrix (assignment matrix), assignment vector, loss (numeric),
  and k (latent dimension).
}
\description{
GRASP-specific method for pairwise alignment using spectral bases and
descriptor alignment with an orthogonal rotation matrix.
}
\examples{
\donttest{
set.seed(123)
X1 <- matrix(rnorm(100), 50, 2)
X2 <- matrix(rnorm(100), 50, 2)
algo <- grasp_aligner()
fit <- fit_pair(algo, X1, X2, ncomp = 5, q_descriptors = 10)
class(fit)
}
}
