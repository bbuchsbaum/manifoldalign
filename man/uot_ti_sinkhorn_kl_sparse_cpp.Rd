% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{uot_ti_sinkhorn_kl_sparse_cpp}
\alias{uot_ti_sinkhorn_kl_sparse_cpp}
\title{KL translation-invariant Sinkhorn (sparse kNN cost)}
\usage{
uot_ti_sinkhorn_kl_sparse_cpp(
  row_ptr,
  col_idx,
  cost,
  n_rows,
  n_cols,
  alpha,
  beta,
  epsilon,
  rho1,
  rho2,
  max_iter = 2000L,
  tol = 1e-06
)
}
\arguments{
\item{row_ptr}{Integer vector of length n+1 with CSR row offsets (0-based or
1-based accepted; if 1-based, it must start at 0 or 1 and be nondecreasing).}

\item{col_idx}{Integer vector of length nnz giving 1-based column indices.}

\item{cost}{Numeric vector of length nnz giving cost values aligned with
`col_idx`.}

\item{n_rows}{Number of source nodes (n).}

\item{n_cols}{Number of target nodes (m).}

\item{alpha}{Source masses (length n).}

\item{beta}{Target masses (length m).}

\item{epsilon}{Entropic regularization parameter (> 0).}

\item{rho1}{KL penalty on the first marginal (> 0).}

\item{rho2}{KL penalty on the second marginal (> 0).}

\item{max_iter}{Maximum number of iterations.}

\item{tol}{Stopping tolerance on the infinity-norm iterate difference.}
}
\value{
A list with translation-invariant potentials `fbar`, `gbar`,
  translated dual potentials `f`, `g`, translation `lambda`, iteration
  count, convergence flag, and last residual.
}
\description{
Sparse CPU implementation using a CSR-like row pointer representation.
}
