% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kema_orig.R
\name{kema_orig}
\alias{kema_orig}
\alias{kema_orig.multidesign}
\alias{kema_orig.hyperdesign}
\alias{kema_orig.default}
\title{Original Kernel Manifold Alignment (KEMA)}
\usage{
kema_orig(data, y, ...)

\method{kema_orig}{multidesign}(
  data,
  y,
  subject,
  preproc = center(),
  ncomp = 2,
  knn = 5,
  sigma = 0.73,
  mu = 0.5,
  kernel = coskern(),
  sample_frac = 1,
  lambda = 0,
  backend = "auto",
  backend_control = NULL,
  ...
)

\method{kema_orig}{hyperdesign}(
  data,
  y,
  preproc = center(),
  ncomp = 2,
  knn = 5,
  sigma = 0.73,
  mu = 0.5,
  kernel = coskern(),
  sample_frac = 1,
  lambda = 0,
  backend = "auto",
  backend_control = NULL,
  ...
)

\method{kema_orig}{default}(data, ...)
}
\arguments{
\item{data}{Input object. A `hyperdesign` or `multidesign` object.}

\item{y}{Label column used for class graphs.}

\item{...}{Method-specific arguments.}

\item{subject}{Subject/domain column for splitting a multidesign object.}

\item{preproc}{Preprocessing recipe (default `center()`).}

\item{ncomp}{Number of components.}

\item{knn}{Number of nearest neighbors for within-domain graph.}

\item{sigma}{Kernel graph scale for local geometry.}

\item{mu}{Weight for class-pull Laplacian in \eqn{L + \mu L_s}.}

\item{kernel}{Kernel function.}

\item{sample_frac}{Fraction of samples retained as landmarks (<1 => REKEMA).}

\item{lambda}{Non-negative regularization added to RHS generalized matrix.
Set to `0` for strict paper formulation.}

\item{backend}{Backend for the generalized eigensolver. One of `"auto"`,
`"full_exact"`, `"reduced_exact"`, or `"operator_exact"`.}

\item{backend_control}{Optional list for backend auto-selection and fidelity
diagnostics. Supported keys: `full_exact_max_n`, `reduced_exact_max_r`,
`fidelity_residual_tol`, `fidelity_orth_tol`, `fidelity_action`,
`primme_tol`, and `primme_method`.}
}
\value{
A `multiblock_biprojector` object with KEMA embeddings.
}
\description{
Paper-faithful implementation of Tuia & Camps-Valls (2016) using the
generalized eigenproblems:
}
\details{
Full KEMA (Eq. 6):
\deqn{K (L + \mu L_s) K \Lambda = \lambda K L_d K \Lambda}

REKEMA (Eq. 10, reduced-rank):
\deqn{K_{rn} (L + \mu L_s) K_{nr} \Lambda = \lambda K_{rn} L_d K_{nr} \Lambda}

This function intentionally omits extension layers (regression solver,
repulsion term \eqn{L_r}, fail-soft retries, and kernel centering variants).
}
\references{
Tuia, D., & Camps-Valls, G. (2016). Kernel manifold alignment for domain
adaptation. PLoS ONE, 11(2), e0148655.
}
