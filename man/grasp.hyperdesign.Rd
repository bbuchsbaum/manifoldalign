% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grasp.R
\name{grasp.hyperdesign}
\alias{grasp.hyperdesign}
\title{Graph Alignment by Spectral Corresponding Functions (GRASP) - Corrected Version}
\usage{
\method{grasp}{hyperdesign}(
  data,
  preproc = multivarious::center(),
  ncomp = 30,
  q_descriptors = 100,
  sigma = 0.73,
  lambda = 0.1,
  use_laplacian = TRUE,
  solver = "linear",
  ...
)
}
\arguments{
\item{data}{A hyperdesign object containing multiple graph domains}

\item{preproc}{Preprocessing function to apply to the data (default: center)}

\item{ncomp}{Number of spectral components to extract (default: 30)}

\item{q_descriptors}{Number of diffusion-time descriptors (default: 100)}

\item{sigma}{Diffusion parameter for descriptor computation (default: 0.73)}

\item{lambda}{Regularization parameter for base alignment (default: 0.1)}

\item{use_laplacian}{Whether to use Laplacian normalization (default: TRUE)}

\item{solver}{Assignment method: "linear" for exact assignment (default)}

\item{...}{Additional arguments (currently unused)}
}
\value{
A multiblock_biprojector object containing the GRASP alignment
}
\description{
Performs GRASP alignment on hyperdesign data structures. 
Projects graph data from multiple domains into aligned spectral spaces.
}
\examples{
\donttest{
# Example with hyperdesign graph data
library(multidesign)
library(tibble)

# Create synthetic graph domains (node coordinates for graph construction)
set.seed(123)
X1 <- matrix(rnorm(100), 50, 2)  # Node coordinates for domain 1
X2 <- matrix(rnorm(100), 50, 2)  # Node coordinates for domain 2

# Create design data frames with node indices (GRASP doesn't use features)
design1 <- tibble(node_id = 1:50)
design2 <- tibble(node_id = 1:50)

# Create multidesign objects
md1 <- multidesign(X1, design1)
md2 <- multidesign(X2, design2)

# Create hyperdesign from multidesign objects
hd <- hyperdesign(list(domain1 = md1, domain2 = md2))

# Run GRASP alignment (purely spectral, uses graph structure from X)
result <- grasp(hd, ncomp = 20, q_descriptors = 50)
}

}
