% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uot_multiset_align.R
\name{uot_ti_sinkhorn_kl}
\alias{uot_ti_sinkhorn_kl}
\title{Translation-invariant unbalanced OT (KL) via TI-Sinkhorn}
\usage{
uot_ti_sinkhorn_kl(
  cost,
  alpha,
  beta,
  epsilon,
  rho1,
  rho2,
  max_iter = 2000,
  tol = 1e-06
)
}
\arguments{
\item{cost}{A dense numeric matrix (n x m) or a sparse cost list. Sparse
costs must include CSR fields `row_ptr`, `col_idx`, `cost`, `n_rows`,
`n_cols`. If CSC fields `col_ptr`, `row_idx`, `cost_csc` are present they
are used for faster updates.}

\item{alpha}{Source masses (length n, nonnegative).}

\item{beta}{Target masses (length m, nonnegative).}

\item{epsilon}{Entropic regularization parameter (> 0).}

\item{rho1}{KL penalty on the first marginal (> 0).}

\item{rho2}{KL penalty on the second marginal (> 0).}

\item{max_iter}{Maximum number of TI-Sinkhorn iterations.}

\item{tol}{Stopping tolerance on iterate change.}
}
\value{
A list with translation-invariant potentials `fbar`, `gbar`, translated dual
potentials `f`, `g`, translation `lambda`, `iterations`, `converged`, and
`residual`.
}
\description{
Solve entropic unbalanced OT with KL marginal penalties using the
translation-invariant (TI) Sinkhorn updates from Séjourné, Vialard, Peyré
(AISTATS 2022). Supports dense costs and sparse neighbourhood graphs.
}
\examples{
set.seed(1)
C <- matrix(runif(30), 5, 6)
a <- rep(1, 5)
b <- rep(1, 6)
fit <- uot_ti_sinkhorn_kl(C, a, b, epsilon = 0.5, rho1 = 1, rho2 = 1,
                          max_iter = 200, tol = 1e-8)
str(fit)

}
