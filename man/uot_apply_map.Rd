% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uot_multiset_align.R
\name{uot_apply_map}
\alias{uot_apply_map}
\title{Apply a UOT coupling to map signals into template space}
\usage{
uot_apply_map(cost, alpha, beta, fbar, gbar, epsilon, signal, delta = 1e-08)
}
\arguments{
\item{cost}{A dense numeric matrix (n x m) or a sparse cost list with CSR
fields from \code{\link{uot_build_cost}}.}

\item{alpha}{Source masses (length n).}

\item{beta}{Target masses (length m).}

\item{fbar}{Translation-invariant source potential (length n).}

\item{gbar}{Translation-invariant target potential (length m).}

\item{epsilon}{Entropic regularization parameter (> 0).}

\item{signal}{Source signal: numeric vector (length n) or matrix (T x n).}

\item{delta}{Stabilizer added to the denominator.}
}
\value{
A numeric vector (length m) or matrix (T x m).
}
\description{
Applies the entropic UOT coupling implied by translation-invariant dual
potentials, without materializing the full transport plan.
}
\examples{
set.seed(1)
C <- matrix(runif(30), 5, 6)
a <- rep(1, 5)
b <- rep(1, 6)
fit <- uot_ti_sinkhorn_kl(C, a, b, epsilon = 0.5, rho1 = 1, rho2 = 1,
                          max_iter = 200, tol = 1e-8)
s <- rnorm(5)
y <- uot_apply_map(C, a, b, fit$fbar, fit$gbar, epsilon = 0.5, signal = s)
str(y)

}
