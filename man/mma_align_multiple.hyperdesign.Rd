% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mma_align_multiple.R
\name{mma_align_multiple.hyperdesign}
\alias{mma_align_multiple.hyperdesign}
\title{Multiset Manifold Alignment (MMA)}
\usage{
\method{mma_align_multiple}{hyperdesign}(
  data,
  ref_idx = 1L,
  preproc = center(),
  ncomp = 10,
  sigma = 0.73,
  knn = NULL,
  embedding = c("ctd", "lap"),
  normalize = c("hypersphere", "none"),
  hist_bins = NULL,
  hist_max_bins = 128L,
  hist_similarity = c("cor", "cosine"),
  signature_method = c("hist", "w2", "hybrid"),
  eig_penalty = 0,
  signature_gate_multiplicity = FALSE,
  signature_gate_tau = NULL,
  signature_retry_if_weak = FALSE,
  em_max_iter = 50L,
  em_tol = 1e-05,
  em_sigma0 = NULL,
  em_outlier = 0.01,
  force_SO = FALSE,
  target_var = 0.95,
  match_to = c("reference", "consensus"),
  consensus_centers = c("ref", "min"),
  n_centers = NULL,
  consensus_init = c("ref", "kmeans"),
  final_assignment = c("none", "hungarian", "argmax"),
  ...
)
}
\arguments{
\item{data}{A hyperdesign object containing three or more domains}

\item{ref_idx}{Integer; which domain acts as reference (default 1)}

\item{preproc}{Preprocessing function (default multivarious::center())}

\item{ncomp}{Number of components; set NULL to choose automatically}

\item{sigma}{Gaussian affinity bandwidth (in data units)}

\item{knn}{Number of nearest neighbors for graph construction (default adaptive)}

\item{embedding}{One of "ctd" (commute-time; default) or "lap"}

\item{normalize}{One of "hypersphere" (default) or "none" for embeddings}

\item{hist_bins}{Integer number of bins for eigensignature histograms; NULL for auto}

\item{hist_max_bins}{Clamp for auto-bins (default 128)}

\item{hist_similarity}{One of "cor" or "cosine" (default "cor")}

\item{signature_method}{One of "hist", "w2", or "hybrid" for eigenvector matching}

\item{eig_penalty}{Eigenvalue penalty weight for signature alignment (default 0)}

\item{signature_gate_multiplicity}{Logical; gate matches by eigenvalue clusters}

\item{signature_gate_tau}{Threshold for eigenvalue clustering (NULL for auto)}

\item{signature_retry_if_weak}{Logical; retry with alternative method if weak match}

\item{em_max_iter}{Integer EM iterations (default 50)}

\item{em_tol}{Numeric EM tolerance on relative change (default 1e-5)}

\item{em_sigma0}{Optional initial stddev in embedding units; NULL for auto}

\item{em_outlier}{Prior mass for uniform outlier component (default 0.01)}

\item{force_SO}{Logical; if TRUE, force det(R)=+1 (default FALSE)}

\item{target_var}{Lower bound criterion for auto K when ncomp=NULL (default 0.95)}

\item{match_to}{One of "reference" (default) or "consensus"}

\item{consensus_centers}{Strategy to set number of consensus centers when
match_to="consensus": "ref" (use ref domain size) or "min" (use minimum size)}

\item{n_centers}{Optional integer overriding consensus_centers; must be <= min sizes}

\item{consensus_init}{Initialization for consensus centers: "ref" or "kmeans"}

\item{final_assignment}{Optional post-processing: "none" (default), "hungarian",
or "argmax". Affects only extras$assignment; core EM remains soft.}

\item{...}{Unused.}
}
\value{
A multiblock alignment object with stacked scores, block loadings,
  and extras (rotations, posteriors, histogram alignment info, and optionally
  consensus and final assignment).
}
\description{
Aligns 3+ domains by: (i) building spectral embeddings (commute-time or
Laplacian), (ii) aligning eigenbases via eigensignatures (histograms +
Hungarian), and (iii) refining per-domain alignments with an EM-based
probabilistic point-registration on the orthogonal group.
}
\details{
Two matching modes are supported:
- match_to = "reference": pairwise EM to a chosen reference domain.
- match_to = "consensus": joint EM that learns a shared template (consensus centers)
  along with per-domain orthogonal transforms; supports unequal node counts naturally.

The implementation reuses shared helpers in this package:
- adjoin::graph_weights + adjacency for graph building
- graph_laplacian (internal) for Laplacian construction
- safe_compute, compute_block_indices, feature_block_indices, new_alignment_result
}
\examples{
\donttest{
set.seed(1)
X1 <- matrix(rnorm(30), 10, 3)
X2 <- matrix(rnorm(30), 10, 3)
X3 <- matrix(rnorm(30), 10, 3)
data_list <- list(X1, X2, X3)
result <- mma_align_multiple(data_list, ncomp = 2)
dim(result$scores)
}
}
