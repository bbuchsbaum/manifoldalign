% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiscale_manifold_align_control.R
\name{multiscale_manifold_align_control}
\alias{multiscale_manifold_align_control}
\title{Control settings for multiscale manifold alignment backends}
\usage{
multiscale_manifold_align_control(
  enabled = FALSE,
  backend = c("spectral", "randomized_dwt"),
  operator_mode = c("compressed_graph_core"),
  rank_per_domain = 128L,
  svd_tol = 1e-06,
  eigen_tol = 1e-08,
  max_levels = 8L,
  eps_rank = 0.001,
  eigen_solver = c("lobpcg", "lanczos"),
  eigen_k_max = 512L,
  knn = 12L,
  sigma = NULL,
  cross_edge_weight = 1,
  mnn_k = 5L,
  max_pairs_per_label = 100L,
  max_unsup_pairs_per_pair = 200L,
  randomized_sketch_size = 256L,
  randomized_oversample = 16L,
  randomized_power_iters = 1L,
  shift_invert_delta = 0.001,
  cg_tol = 1e-06,
  cg_max_iter = 500L,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{enabled}{Logical; enable multiscale mode.}

\item{backend}{Character backend name. One of `"spectral"` or `"randomized_dwt"`.}

\item{operator_mode}{Character operator mode. Currently `"compressed_graph_core"`.}

\item{rank_per_domain}{Integer per-domain truncation rank for compressed operators.}

\item{svd_tol}{Numeric positive tolerance for truncated SVD decisions.}

\item{eigen_tol}{Numeric positive tolerance for non-trivial eigenvalues.}

\item{max_levels}{Integer maximum hierarchy depth.}

\item{eps_rank}{Numeric relative truncation threshold in `(0, 1]`.}

\item{eigen_solver}{Character eigensolver selector. One of `"lobpcg"` or `"lanczos"`.}

\item{eigen_k_max}{Integer cap on eigenvectors for spectral hierarchy.}

\item{knn}{Integer nearest-neighbor graph degree for within-domain graphs.}

\item{sigma}{Optional positive scalar graph bandwidth. If `NULL`, auto-tuned per domain.}

\item{cross_edge_weight}{Numeric non-negative weight for cross-domain correspondence edges.}

\item{mnn_k}{Integer nearest-neighbor count for unsupervised MNN correspondences.}

\item{max_pairs_per_label}{Integer cap per label/pair when building label correspondences.}

\item{max_unsup_pairs_per_pair}{Integer cap per domain pair in unsupervised MNN mode.}

\item{randomized_sketch_size}{Integer sketch size for randomized backend.}

\item{randomized_oversample}{Integer oversampling budget for randomized backend.}

\item{randomized_power_iters}{Integer number of subspace power iterations.}

\item{shift_invert_delta}{Numeric positive regularization for shift-invert style operators.}

\item{cg_tol}{Numeric positive tolerance for iterative solves.}

\item{cg_max_iter}{Integer maximum iterations for iterative solves.}

\item{seed}{Integer random seed.}

\item{verbose}{Logical; print backend progress messages.}
}
\value{
A list with class `multiscale_manifold_align_control`.
}
\description{
Control settings for multiscale manifold alignment backends
}
