# Handle formula interface for supervised embedding

Handle formula interface for supervised embedding

## Usage

``` r
.handle_formula_interface(
  formula,
  data,
  T,
  M,
  sigma_P,
  ncomp,
  alpha_p,
  alpha_schedule,
  maxit,
  tol,
  batch_size,
  use_cpp,
  verbose,
  lr,
  ...
)
```

## Value

A simembed object with formula and response components added
