# Mass-constrained partial OT oracle

Mass-constrained partial OT oracle

## Usage

``` r
partial_ot_mass(cost, p, q, rho, method = c("lpSolve", "Rsymphony"))
```

## Value

A numeric matrix representing the optimal transport plan.
