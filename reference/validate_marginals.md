# Validate marginal distributions

Validate marginal distributions

## Usage

``` r
validate_marginals(marginals, n_samples, n_domains)
```

## Value

A list of validated and normalized marginal distribution vectors, each
summing to 1.
