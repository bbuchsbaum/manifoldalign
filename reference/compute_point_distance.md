# Compute distances between a point and all rows of a matrix

Compute distances between a point and all rows of a matrix

## Usage

``` r
compute_point_distance(point, data, metric, data_norm = NULL)
```

## Value

A numeric vector of distances from the query point to each row of
\`data\`.
