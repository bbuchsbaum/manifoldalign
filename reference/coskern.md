# Cosine kernel function

Cosine kernel function

## Usage

``` r
coskern()
```

## Value

A kernel object for cosine similarity

## Examples

``` r
kern <- coskern()
x <- c(1, 2, 3)
y <- c(4, 5, 6)
# kern(x, y)  # Compute cosine similarity
```
