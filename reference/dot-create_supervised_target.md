# Create supervised target similarity matrix

Create supervised target similarity matrix

## Usage

``` r
.create_supervised_target(labels)
```

## Arguments

- labels:

  Factor or character vector of class labels

## Value

Binary similarity matrix (1 if same class, 0 otherwise)
